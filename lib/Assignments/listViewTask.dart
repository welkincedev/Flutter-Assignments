import 'package:flutter/material.dart';

class ListViewTask extends StatelessWidget {
  const ListViewTask({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("List View Task")),
      body: Placeholder(),
    );
  }
}
